{
  "name": "nuxt-cookie-storage",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "js-cookie": "^2.2.0"
  },
  "devDependencies": {
    "coffeescript": "^2.2.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "coffee --compile --output lib/ src/",
    "dev": "coffee --watch --compile --output lib/ src/"

  },
  "author": "",
  "license": "ISC"
}
